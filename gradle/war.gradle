apply plugin: "war"

bootWar {
    mainClassName = "ro.trc.office.organization.OrganizationApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]
}
